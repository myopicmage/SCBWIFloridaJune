@model IEnumerable<SCBWIFloridaJune.Models.Price>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PriceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PriceID })
        </td>
    </tr>
}

</table>

<p>
    @Html.ActionLink("Back to list", "Index", "Manage")
</p>